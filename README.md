## Для передачи имеются два параметра:
- URL 
- BROWSER

URL: является обязательным для указания
BROWSER: по умолчанию в переменной стоит хром

Пример

```sh
clean task reg_form -Durl=${URL} -Dbrowser=${BROWSER}
```
